{
  "name": "desafio-final",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.11.15",
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /desafio-final-m5/",
    "deploy": "gh-pages -d build "
  }
}
